This directory can contain vendored MMFF parameter files (e.g. ``MMFF94.ff``).
Drop the files here if the runtime environment does not provide the RDKit data
share.  The loader automatically scans this directory as a last resort.
